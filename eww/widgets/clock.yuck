;; Clock and Date widget
(defwidget clock-section []
  (box :class "clock-section" :orientation "v" :space-evenly false :spacing 2
    (label :class "clock-time" :text "${formattime(EWW_TIME, clock-format)}")
    (label :class "clock-date" :text "${formattime(EWW_TIME, date-format)}")))